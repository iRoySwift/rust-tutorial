use crate_restaurant;

fn main() {
    // crate_restaurant::front_of_house::hosting::add_to_waitlist();
    // pub use
    crate_restaurant::hosting::add_to_waitlist();
    crate_restaurant::run();
}
